import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 0, 0, 0);
  static const Color accent = Color(0xFF8CB369);
  static const Color accentDark = Color(0xFF5E8C61);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}

class AppFonts {
  static const String type = 'Manrope';
  static const double sizeSmall = 12.0; // Use for captions or small text
  static const double sizeMedium = 14.0; // Use for body text or standard text
  static const double sizeLarge =
      16.0; // Use for subheadings or important information
  static const double sizeXLarge = 20.0; // Use for headings or titles
  static const double sizeXXLarge =
      24.0; // Use for main headings or prominent titles
}
